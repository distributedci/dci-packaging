#!/usr/bin/expect

set timeout 3600
set path [lindex $argv 0];
set project [lindex $argv 1];
set path_to_repo [lindex $argv 2];

spawn ./build_rpm.sh $path $project $path_to_repo
expect "Enter pass phrase: "
send -- "\r"
expect eof
